machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t tester -f cluster/docker/tester_Dockerfile .

test:
  override:
    - docker run tester "/bin/bash -c nose2"

